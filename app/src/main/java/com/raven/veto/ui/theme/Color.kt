package com.raven.veto.ui.theme

import androidx.compose.ui.graphics.Color

// Veto's signature mint green - the cat's signature color
val VetoMint20 = Color(0xFF0F4A3F) // Dark mint for dark theme backgrounds
val VetoMint40 = Color(0xFF1B7A68) // Medium mint for light theme
val VetoMint60 = Color(0xFF4BA895) // Lighter mint for light theme elements
val VetoMint80 = Color(0xFFA8E6D6) // Bright mint for dark theme primary
val VetoMint90 = Color(0xFFC8F2E8) // Very light mint for light theme backgrounds

// Veto's dark side - the playful black cat
val VetoBlack = Color(0xFF1A1A1A) // Pure black for backgrounds
val VetoGray = Color(0xFF2A2A2A) // Gray for surfaces

// Light theme gray backgrounds
val VetoLightBackground = Color(0xFFF8FFFE) // Almost white with mint tint
val VetoLightSurface = Color(0xFFFFFFFF) // Pure white

// Accent colors for depth
val VetoTeal = Color(0xFF4A9B8E)

// Anki standard colors (keep these as-is for consistency)
val AnkiNewBlue = Color(0xFF2196F3)
val AnkiLearnRed = Color(0xFFF44336)
val AnkiReviewGreen = Color(0xFF4CAF50)
